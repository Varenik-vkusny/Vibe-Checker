# ✨ Vibe Checker

**Vibe Checker** — это AI-платформа для глубокого анализа и поиска заведений. Мы выходим за рамки обычных "звездных рейтингов", анализируя сотни текстовых отзывов с помощью нейросетей, чтобы определить настоящую атмосферу ("вайб") места.

![Project Status](https://img.shields.io/badge/Status-Active-success)
![Python](https://img.shields.io/badge/Backend-FastAPI-blue)
![TypeScript](https://img.shields.io/badge/Frontend-Next.js-black)
![AI](https://img.shields.io/badge/AI-Gemini%20%2B%20Vector%20Search-purple)

## 🚀 Основные возможности

*   **Pro Mode (AI Поиск):** Поиск мест по естественному описанию (например, *"Тихое кафе с быстрым вайфаем для работы"* или *"Шумный бар с техно музыкой"*). Использует векторный поиск (Qdrant) и Reranking.
*   **Глубокий анализ:** Вставьте ссылку на Google Maps, и AI проанализирует отзывы, выделит плюсы/минусы, оценит уровень шума, освещения и цены.
*   **Сравнение (Versus):** Сравните два заведения бок о бок. AI определит победителя в категориях "Еда", "Сервис", "Атмосфера".
*   **Персонализация:** Настройка профиля (предпочтения по шуму, свету, бюджету). Система адаптирует выдачу под ваш "Vibe DNA".
*   **Интерактивная карта:** Интеграция с 2GIS MapGL / Leaflet для отображения результатов.
*   **Админ-панель:** Управление пользователями, просмотр логов и аналитики системы.

## 🛠 Технический стек

### Backend
*   **Язык:** Python 3.13
*   **Фреймворк:** FastAPI
*   **База данных:** PostgreSQL (основные данные), Redis (кеширование и брокер задач).
*   **Vector DB:** Qdrant (хранение эмбеддингов мест для семантического поиска).
*   **ORM:** SQLAlchemy (Async).
*   **Асинхронные задачи:** Celery + Redis.
*   **AI & LLM:** Google Gemini (модель Flash-Lite), Sentence Transformers.
*   **Парсинг:** SerpApi (Google Maps Reviews).

### Frontend
*   **Фреймворк:** Next.js 15 (App Router).
*   **Язык:** TypeScript.
*   **Стилизация:** Tailwind CSS, Shadcn UI.
*   **Анимации:** Framer Motion, GSAP.
*   **Карты:** 2GIS MapGL API, React-Leaflet.
*   **State Management:** React Context + Hooks.

### Инфраструктура
*   **Контейнеризация:** Docker, Docker Compose.

---

## ⚙️ Установка и запуск

Проект полностью контейнеризирован. Для запуска вам понадобятся **Docker** и **Docker Compose**.

### 1. Клонирование репозитория

```bash
git clone https://github.com/Varenik-vkusny/Vibe-Checker.git
cd vibe-checker
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта. Вы можете использовать пример ниже:

```ini
# .env

# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
DB_NAME=vibe_db

# Security
SECRET_KEY=super_secret_key_change_me_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# API Keys (Required for AI features)
GEMINI_API_KEY=your_google_gemini_key
SERPAPI_KEY=your_serpapi_key

# Services
REDIS_URL=redis://redis:6379/0
# Qdrant (Vector DB)
COLLECTION_NAME=places_collection
# Inference Service (внешний или локальный сервис для эмбеддингов)
INFERENCE_API_URL=http://your-inference-service-or-mock

# Environment
ENV=dev
```

### 3. Запуск через Docker Compose

Эта команда соберет бэкенд, фронтенд, воркеры и поднимет базы данных.

```bash
docker-compose up --build
```

После успешного запуска сервисы будут доступны по адресам:

*   **Frontend:** [http://localhost:3000](http://localhost:3000)
*   **Backend API:** [http://localhost:8000](http://localhost:8000)
*   **Swagger Docs:** [http://localhost:8000/docs](http://localhost:8000/docs)
*   **Qdrant Dashboard:** [http://localhost:6333/dashboard](http://localhost:6333/dashboard)

---

## 💻 Локальная разработка (без Docker для кода)

Если вы хотите запустить базы данных в Docker, а код запускать локально для отладки:

### Бэкенд

1.  Запустите базы данных:
    ```bash
    docker-compose up -d db redis qdrant
    ```
2.  Создайте виртуальное окружение и установите зависимости:
    ```bash
    python -m venv venv
    source venv/bin/activate  # или venv\Scripts\activate на Windows
    pip install -r requirements.txt
    ```
3.  Убедитесь, что в `.env` хосты баз данных (`DB_HOST`, `REDIS_URL`) указывают на `localhost` (так как порты проброшены).
4.  Запустите сервер:
    ```bash
    uvicorn app.main:app --reload
    ```

### Фронтенд

1.  Перейдите в папку фронтенда:
    ```bash
    cd app/frontend
    ```
2.  Установите зависимости:
    ```bash
    npm install
    # или
    yarn install
    ```
3.  Запустите сервер разработки:
    ```bash
    npm run dev
    ```

---

## 📂 Структура проекта

```text
.
├── app/
│   ├── celery_app.py        # Настройки Celery
│   ├── main.py              # Точка входа FastAPI
│   ├── endpoints/           # API Роуты (users, place, admin...)
│   ├── modules/             # Бизнес-логика (services, repos, models)
│   │   ├── pro_mode/        # Логика AI поиска и LLM
│   │   ├── parsing/         # Парсеры (SerpApi)
│   │   └── ...
│   ├── services/            # Оркестраторы (analyzator, comparator)
│   └── frontend/            # Next.js приложение
│       ├── app/             # App Router страницы
│       ├── components/      # UI компоненты
│       └── lib/             # Утилиты и API клиенты
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── .env
```

## 🧪 Тестирование

Бэкенд использует `pytest`. Тесты находятся в папке `app/tests`.

```bash
# Запуск тестов (требует установленных зависимостей)
pytest
```

## 🤝 Вклад в проект (Contributing)

1.  Форкните репозиторий.
2.  Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`).
3.  Закоммитьте изменения (`git commit -m 'Add amazing feature'`).
4.  Запушьте ветку (`git push origin feature/amazing-feature`).
5.  Откройте Pull Request.
